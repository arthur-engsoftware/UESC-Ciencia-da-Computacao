#include <stdio.h>

int main (void)
{
   int a, b,c ,d, dif;
    scanf("%i",&a);
    scanf("%i",&b);
    scanf("%i",&c);
    scanf("%i",&d);
    dif = (a*b) - (c*d);
    printf ("DIFERENCA = %i", dif);
    return 0;
}